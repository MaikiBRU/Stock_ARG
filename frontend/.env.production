# Variables publicas del build de produccion. Terminan en el JavaScript
# que se sirve, asi que aca nunca va un secreto.
NEXT_PUBLIC_API_URL=https://api-stockarg.aaronbrumat.com.ar
# Completar cuando exista el cliente OAuth web de Google (es publico).
# NEXT_PUBLIC_GOOGLE_CLIENT_ID=
